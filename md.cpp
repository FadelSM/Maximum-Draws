#include <bits/stdc++.h>
using namespace std;

// Ini fungsi yang kamu kerjakan (10 poin tadi)
int maximumDraws(int n) {
    return n + 1;
}

// INI YANG HILANG/ERROR. Jangan dihapus!
int main() {
    int t;
    cin >> t; // Membaca jumlah test case
    while (t--) {
        int n;
        cin >> n; // Membaca input n
        cout << maximumDraws(n) << endl; // Cetak hasil
    }
    return 0;
}
